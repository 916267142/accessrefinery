# STATUS

Our artifact aligns with the focus of the FSE 2026 Artifacts Track as follows:

- **Software:** We provide the implementation of **AccessRefinery** and a reimplementation of **AWS Access Analyzer** (as a baseline), which enables follow-up studies.
- **Frameworks:** We design the Multi-theory Constraint Preprocessor (**MCP**) as a data structure that accelerates multi-round SMT solving. We also package **MCP** as a Java JAR to facilitate reuse.
- **Datasets:** We provide three open-source datasets synthesized from real-world data.

We apply for the following FSE 2026 Artifact Evaluation badges:

## Evaluated - Functional

We believe this artifact satisfies the Functional criteria (*The artifacts associated with the research are found to be documented, consistent, complete, exercisable, and include appropriate evidence of verification and validation.*) based on the following evidence:

- **Documented:** The artifact provides documentation on:
  - Requirements
  - Installation
  - Project structure
  - Usage and examples
  - API documentation generated by Javadoc
  - Reproduction scripts and instructions
  - Running instructions in vscode IDEA for developers
- **Exercisable:** Users can build and execute the system from source using standard Maven workflows with Java JDK 17.
- **Complete:** The artifact includes all materials required to reproduce the reported experiments:
  - Source code for **AccessRefinery**
  - Source code for our reimplementation of **Access Analyzer**
  - Scripts for invoking **AWS Access Analyzer** via the CLI API (**AWS Access Analyzer**, our baseline approach, is not open source and only provides a CLI API.)
  - Three synthetic datasets
  - Reproduction scripts and instructions
  - Plotting scripts
- **Consistency and Evidence:** The released artifact is consistent with the evaluation conclusions reported in the paper. We provide archived results and instructions for mapping those results to the paper's conclusions.

## Evaluated - Reusable

We believe this artifact satisfies the Reusable criteria (*They are very carefully documented and well-structured to the extent that reuse and repurposing is facilitated.*) for the following reasons:

- **Reuse and Repurposing of MCP (Library-Level Reuse):** We provide the following evidence to support direct reuse and extension of **MCP** in other projects:
  - **Modular Design:** **MCP** is implemented as an independent Maven module. Running `$ mvn package` at the repository root generates `mcp-1.0.jar`, which can be directly imported by other Java projects.
  - **Easy-to-use Java API and Examples:** **MCP** supports Java-style chained Boolean operations, for example, `policy.not().and(intent1.or(intent2))`. We also provide usage examples.
  - **General Design:** **MCP** uses a general design for multiple variable types, including regex (RegexpLabel.java), prefix (PrefixLabel.java), range (RangeLabel.java), and set (IntegerSetLabel.java). These types are mapped to a unified superclass (Label.java) via Java polymorphism. **MCP** then performs processing (e.g., equivalence-class partitioning and bit-vector encoding) on the unified superclass. This design facilitates extension to additional variable types.
  - **Standardized API Documentation:** We provide standardized comments in the MCP source code, enabling automatic Maven documentation generation.

- **Reuse and Repurposing of AccessRefinery and Access Analyzer (Tool-Level Reuse)**: We provide the following evidence to support practical reuse in intent-mining and comparison workflows:
  - **Easy-to-use Command-Line API and Examples:** Running `$ mvn package` at the repository root generates `accessrefinery-1.0.jar` and `accessanalyzer-1.0.jar`. For each JAR package, we provide command-line parsing support, so developers do not need to handle internal implementation details when conducting comparison experiments.
  For example, `$ java -jar target/accessrefinery-1.0.jar -m -r --sat --round 10 -f data/Correctness` automatically runs 10 rounds to compute average runtime, and `--sat` indicates that MiniSAT is used instead of BDD to represent bit-vectors.
  We also provide usage examples for both **AccessRefinery** and **Access Analyzer**.
  - **Standardized API Documentation:** We provide standardized comments in the AccessRefinery source code, enabling automatic Maven documentation generation.

## Available

We believe this artifact satisfies the Available criteria (*A DOI or link to this repository along with a unique identifier for the object is provided.*) for the following reasons:

The artifact is publicly available in two ways:

<!-- @916267142 Add Zenodo and Github Link -->

- Archival release: a [Zenodo]() snapshot version with DOI 1111:1111.
- Development release: a [GitHub]() repository version.
